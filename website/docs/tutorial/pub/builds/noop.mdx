# NOOP



You can now build **pure data* archive by noop build
Simply set the `builder` to `noop` and set the `buildmode` you want:

:::info
  since `v0.6.21`
:::
  
```yaml title=".kmdopkg.yaml"
builds:
  - builder: noop
```
